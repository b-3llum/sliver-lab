# Path to a Sliver operator config (.cfg). If unset, the backend uses the first
# *.cfg under ~/.sliver-client/configs/.
SLIVER_CFG_PATH=

# Backend host/port (the dev script binds to 127.0.0.1 by default).
HOST=127.0.0.1
PORT=8000
